---

- name: Converge
  hosts: all
  become: "{{ molecule['converge']['become'] | default('yes') }}"
  tasks:
    - name: Include role 'ansible-role-virtualbox'
      include_role:
        name: ansible-role-virtualbox
